"use client";

const Header = () => {
  return (
    <header className='flex top-8 left-8 fixed backdrop-filter backdrop-blur-sm justify-between items-center w-[calc(100%-64px)] transition min-h-[64px] rounded px-5'>
      <div>Discord Anime pfp</div>
      <div>Log in</div>
    </header>
  );
};

export default Header;
